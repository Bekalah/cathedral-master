# Recovery Patches (Staging)
Moonchild writes proposed PATCH markdown files here when run in recovery mode.
You then copy/paste approved snippets into the target files, and mark them with:
`<!-- lock:saturn -->`